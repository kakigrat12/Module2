Выполни лабораторную работу №$ARGUMENTS для Android-проекта в папке lb$ARGUMENTS/.

## Задание
Описание лабы — в файле `/Users/andrey/Module2/Модуль 2.docx`. Реализуй ровно то, что там описано — не больше, не меньше.

## Обязательные правила
- Язык: Java. UI: XML Views (не Compose).
- ФИО студента "Зимин Андрей Михайлович" — TextView внизу экрана во всех лабах.
- Если лаба требует options menu с пунктом "О программе" — реализуй AlertDialog с ФИО.
- Если нужен ActionBar/меню — убери `.NoActionBar` из темы в `themes.xml` (обоих файлах: values/ и values-night/).
- Перед правкой файла — сначала прочитай его (Read).
- Все новые зависимости добавляй в `gradle/libs.versions.toml` и `app/build.gradle.kts`.

## Порядок действий
1. Прочитай нужные файлы проекта lb$ARGUMENTS/.
2. Реализуй лабу полностью.
3. Сделай git commit с сообщением "lb$ARGUMENTS: <краткое описание>" из корня `/Users/andrey/Module2/`.
4. Выведи краткое объяснение: что делает приложение + где что находится (файлы).
